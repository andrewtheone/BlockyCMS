{% extends "@theme/_layout.twig" %}

{% block page %}
	{% set pageVars = pageVars|default({})|merge({
		title: 'Regisztráció'
	}) %}
	{{ parent() }}
{% endblock %}

{% block content %}
	{% set contentVars = contentVars|default({})|merge({
		title: 'Regisztráció',
		subtitle: ''
	}) %}

	{{ parent() }}
	{{ forms_ajaxForm('members_signup') }}

{% endblock %}