{% extends app.config.view.layout %}

{% block content %}
	{% set contentVars = contentVars|default({})|merge({
		title: 'Belépés',
		subtitle: ''
	}) %}

		{{ parent() }}
	{{ forms_ajaxForm('members_login') }}

{% endblock %}