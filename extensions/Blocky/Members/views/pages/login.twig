{% extends "@theme/_layout.twig" %}

{% block page %}
	{% set pageVars = pageVars|default({})|merge({
		title: 'Belépés'
	}) %}
	{{ parent() }}
{% endblock %}

{% block content %}
	{% set contentVars = contentVars|default({})|merge({
		title: 'Belépés',
		subtitle: ''
	}) %}

		{{ parent() }}
	{{ forms_ajaxForm('members_login') }}

{% endblock %}