{% extends app.config.view.layout %}

{% block content %}
	{% set contentVars = contentVars|default({})|merge({
		title: 'Adatmódosítás',
		subtitle: ''
	}) %}

	{{ parent() }}
	{{ forms_ajaxForm('members_update') }}

{% endblock %}