{% extends "@theme/_layout.twig" %}

{% block page %}
	{% set pageVars = pageVars|default({})|merge({
		meta_description: 'Kezdőlap',
		meta_keywords: '1,2,3,4,,5,6',
		title: 'Kezdőlap'
	}) %}
	{{ parent() }}
{% endblock %}

{% block content %}
	{% set contentVars = contentVars|default({})|merge({
		title: 'Kezdőlap',
		subtitle: 'Hi'
	}) %}

	{{ parent() }}
	MOBILE
	{% for i in 1..100 %}
	{{ "/themes/default/assets/images/squirell.jpg"|image({quality: 70, size: [200+i,200+i]}) }}<br />
	{% endfor %}
{% endblock %}